export interface ProfileExperience {
  title: string;
  company: string;
  starts_at: string;
  ends_at: string | null;
  description: string | null;
  ai_description?: string | null;
}

// ... rest of the types ...
